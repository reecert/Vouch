.venv/
__pycache__/
*.pyc
.pytest_cache/
.vouch_cache/
*.egg-info/
.DS_Store
.ruff_cache/

# Populated label sets never leave this machine. `eval/labels.yaml` stays tracked and
# EMPTY as the schema contract (a test enforces that); real labelling happens here.
eval/labels.local.yaml
eval/labels.*.local.yaml
